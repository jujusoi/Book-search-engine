#Server

-- replace routes with react router dom & apollo in client
-- add schemas (resolvers & typedefs). replace user-controller, routing with resolvers (queries and mutations)

#Client

-- import apollo requirements in main app
-- create apollo client in main app. wrap App in apolloprovider


-send main file
-  createUser, - post via req body
  getSingleUser, - get by user, user id, param id, or username through params
  saveBook, - put: find one via userId and set savedBooks by req body
  deleteBook, - delete: find one via userId and pull savedBooks by request params bookId
  login, - post: find user by reqbody username or email, verify password, return token/user

